import "./portal.css";
import "./agenda.css";

export const metadata = {
  title: "Portal MC&DJ | ERP interno",
  description: "Portal interno de MC&DJ para agenda NOMIPAQ, roles y operación del despacho."
};

export default function PortalLayout({ children }) {
  return children;
}
